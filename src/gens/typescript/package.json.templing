{
  "name": "{{ project_name }}",
  "version": "{{ project_version }}",
  "main": "main.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/main.js"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}